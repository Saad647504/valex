# If set, the frontend will call the backend directly instead of using a Next.js proxy rewrite
# Example: https://api.yourdomain.com
NEXT_PUBLIC_API_BASE=

# Controls Next.js dev/prod proxy target for /api when NEXT_PUBLIC_API_BASE is not set
API_PROXY_TARGET=http://localhost:5001

# Optional WebSocket URL for Socket.IO (defaults to same origin)
NEXT_PUBLIC_WS_URL=
